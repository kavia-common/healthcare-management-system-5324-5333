# Example environment configuration for FastAPI backend

# MongoDB connection
MONGODB_URL=mongodb://appuser:dbuser123@localhost:5001/?authSource=admin
MONGODB_DB=healthcare

# JWT configuration
JWT_SECRET=change-me
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# CORS allowed origins (comma-separated)
FRONTEND_ORIGINS=http://localhost:3000

# FastAPI port (used by tooling/scripts; uvicorn --port should match)
PORT=3001
